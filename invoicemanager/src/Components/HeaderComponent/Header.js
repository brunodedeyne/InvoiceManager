import React, { Component } from 'react';

//Import CSS
import './Header.css';

class Header extends Component {
  render() {
    return (
        <header></header>
    );
  }
}

export default Header;